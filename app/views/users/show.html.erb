<p id="notice"><%= notice %></p>

<div class="profile">
  <table>
    <thead>
      <tr>
        <th colspan="3">Name</th>
        <th colspan="3">Member Since</th>
      </tr>
    </thead>

    <tbody>
      <tr>
        <td colspan="3"><%= @current_user.name %></td>
        <td colspan="3"><%= @current_user.created_at %></td>
      </tr>
    </tbody>
  </table>
</div>

<%= link_to 'Back', users_path %>
